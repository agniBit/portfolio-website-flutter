name: portfolio
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.1
  percent_indicator: ^2.1.7+2
  google_fonts: ^1.1.1
  scroll_to_index: ^2.0.0
  url_strategy: ^0.2.0
  lottie: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/projectDetails.json
    - assets/17343-programming.json
    - assets/contact.json
